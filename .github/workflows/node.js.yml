# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Mi Primera CI

on:
  push:
    branches: [ "main" ]

jobs:
  mi-primer-job:

    runs-on: ubuntu-latest


    steps:
    - name: 1. Traer mi código
        uses: actions/checkout@v4 # Esta es la acción que clona tu repositorio

      # --- ESTE PASO LO MODIFICAMOS PARA PROBAR ---
      - name: 2. Mostrar Hola y ver archivos
        run: |
          echo "Hello World"
          ls -la # Este comando sirve para listar los archivos en la máquina